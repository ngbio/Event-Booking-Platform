package com.group3.controllers;

public class FinanceController {
//      @RequestMapping("/admin/finance")
//Chứa các Routes (Nhóm 8 - Kế toán & Dòng tiền Admin):
/// /pending-refund (GET: Danh sách sự kiện bị hủy chờ duyệt hoàn tiền)
/// /approve-refund (POST: Form submit duyệt hoàn trả tiền hàng loạt cho khách)
/// /settlements (GET: Danh sách sự kiện kết thúc thành công chờ đối soát)
/// /process-settlement (POST: Form submit duyệt quyết toán cho Nhà tổ chức)
}
